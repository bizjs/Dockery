/// <reference types="vite/client" />

// Injected by vite.config.ts at build time. Holds the app version
// string (git tag, git-describe output, or "dev" as a last resort).
declare const __APP_VERSION__: string;
